# How to install

A. Develop apps:
  1. Clone the repo
     ```sh
     git clone https://github.com/rwndy/tes-awalmula.git
     ```
  2. Install packages
     ```sh
     yarn 
     ```
  3. Start next
     ```sh
     yarn dev
     ```